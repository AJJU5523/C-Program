7.Represent graph using adjacency list/adjacency matrix and perform Depth First Search

#include <iostream>
#include <vector>
#include <stack>
#include <list>
using namespace std;

// Graph Representation using Adjacency Matrix
class GraphMatrix {
    int V;  // Number of vertices
    int** adjMatrix;  // Adjacency Matrix

public:
    GraphMatrix(int vertices) {
        V = vertices;
        adjMatrix = new int*[V];
        for (int i = 0; i < V; i++) {
            adjMatrix[i] = new int[V]{0};  // Initialize matrix with 0 (no edge)
        }
    }

    // Add edge between u and v
    void addEdge(int u, int v) {
        adjMatrix[u][v] = 1;
        adjMatrix[v][u] = 1;  // For undirected graph
    }

    // Depth First Search using adjacency matrix
    void DFS(int start) {
        vector<bool> visited(V, false);
        stack<int> s;
        s.push(start);

        while (!s.empty()) {
            int node = s.top();
            s.pop();

            if (!visited[node]) {
                cout << node << " ";
                visited[node] = true;
            }

            // Push adjacent unvisited nodes onto the stack
            for (int i = 0; i < V; i++) {
